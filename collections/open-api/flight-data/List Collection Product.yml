info:
  name: List Collection Product
  type: http
  seq: 3

http:
  method: GET
  url: https://reqres.in/api/collections?project_id=17253
  headers:
    - name: x-api-key
      value: "{{process.env.xapi-reqres}}"
  params:
    - name: project_id
      value: "17253"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
